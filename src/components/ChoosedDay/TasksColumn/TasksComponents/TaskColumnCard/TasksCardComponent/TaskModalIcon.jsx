

import { Modal } from "./TaskModalIcon.styled"



export const TaskModalIcon = ({children, onClose}) => {
       

    
    

    return (
        
                <Modal>
                {children}
                </Modal>
    )
}